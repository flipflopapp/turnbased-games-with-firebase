{
  "name": "turnbased",
  "version": "1.0.0",
  "description": "Turn based game (currently two players) with firebase (example chess)",
  "scripts": {
    "test": "./test/test-match.sh",
    "test-server": "mocha test/server.js --no-timeouts",
    "test-client": "mocha test/client.js --no-timeouts",
    "start": "node server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "firebase-admin": "^4.0.6"
  },
  "peerDependencies": {
    "firebase": "^3.6.7"
  },
  "devDependencies": {
    "expect": "^1.20.2",
    "mocha": "^3.2.0"
  }
}
